import subprocess

from mahkrab.tools.decorators.timers import *

class Executor:
    @staticmethod
    def exec(targetfile, outputfile, args, runOnCompile) -> None:
        pass
    
    @runtime
    @staticmethod
    def run():
        pass
